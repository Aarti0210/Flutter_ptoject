import 'package:flutter/material.dart';

class Suggession extends StatelessWidget {
  const Suggession({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}